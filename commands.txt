# URL for the app:
https://chat.app.dreamfactory.com


# Login to VM:
gcloud compute ssh --zone "us-central1-f" "appsvm1" --project "ai-benchmarking-project"


# Become root:
sudo su -

# Update code:
cd /opt/dream_factory_evals_app/dream_factory_evals && git pull

# Build Docker Image
cd /opt/dream_factory_evals_app/dream_factory_evals && docker build . -f Dockerfile -t gcr.io/ai-benchmarking-project/apps1/dream_factory_evals-app:v1 && cd /opt/dream_factory_evals_app && docker-compose down && docker-compose up -d

# Check logs:
docker logs -f dream_factory_evals-app

# URL for the app:
https://chat.app.dreamfactory.com
```

*benchmark.app (sglang)*
```
# URL for the app:
https://benchmark.app.dreamfactory.com


ZONE="us-central1-c"
PROJECT_ID="ai-benchmarking-project"
VMNAME="appsvm2"

gcloud compute instances start $VMNAME --zone=$ZONE --project=$PROJECT_ID

ZONE="us-central1-c"
PROJECT_ID="ai-benchmarking-project"
VMNAME="appsvm2"
gcloud compute ssh --zone $ZONE $VMNAME --project $PROJECT_ID


# Become Root
sudo su -


cd /opt/dream_factory_evals_app
docker-compose up -d

cd /opt/dream_factory_evals_app/nginx-certbot
docker-compose up -d

# URL for the app:
https://benchmark.app.dreamfactory.com


# Stop the VM 

ZONE="us-central1-c"
PROJECT_ID="ai-benchmarking-project"
VMNAME="appsvm2"
gcloud compute instances stop $VMNAME --zone=$ZONE --project=$PROJECT_ID


```